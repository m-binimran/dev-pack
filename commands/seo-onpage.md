---
description: Add on-page SEO to a Next.js site — title/meta, Open Graph, canonical, structured data (schema.org JSON-LD), and sitemap/robots. Use when building or auditin...
---

# /seo-onpage $ARGUMENTS

Use the `seo-onpage` skill for this request: $ARGUMENTS

If no details were provided, ask for what the skill needs first, then follow the skill's process exactly.
