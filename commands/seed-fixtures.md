---
description: Generate realistic seed data and reset scripts for local Postgres/Supabase development and tests. Use when setting up a dev database, needing test fixtures,...
---

# /seed-fixtures $ARGUMENTS

Use the `seed-fixtures` skill for this request: $ARGUMENTS

If no details were provided, ask for what the skill needs first, then follow the skill's process exactly.
