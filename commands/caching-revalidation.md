---
description: Get Next.js App Router caching right — static vs dynamic rendering, fetch cache, revalidatePath/revalidateTag, and ISR. Use when data is stale, a page won't...
---

# /caching-revalidation $ARGUMENTS

Use the `caching-revalidation` skill for this request: $ARGUMENTS

If no details were provided, ask for what the skill needs first, then follow the skill's process exactly.
