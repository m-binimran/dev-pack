---
description: Build a production React component on shadcn/ui + Tailwind — variants, compound pattern, accessible states, and a clean prop API. Use when creating a new UI...
---

# /component-scaffold $ARGUMENTS

Use the `component-scaffold` skill for this request: $ARGUMENTS

If no details were provided, ask for what the skill needs first, then follow the skill's process exactly.
