---
description: Safely back up and restore a Postgres/Supabase database — pg_dump/pg_restore, Supabase backups/PITR, and a no-data-loss restore runbook. Use before risky mig...
---

# /db-backup-restore $ARGUMENTS

Use the `db-backup-restore` skill for this request: $ARGUMENTS

If no details were provided, ask for what the skill needs first, then follow the skill's process exactly.
