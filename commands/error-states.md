---
description: Design the non-happy-path UI in Next.js — loading, error, not-found, and empty states with Suspense, error boundaries, and user-facing feedback. Use when a p...
---

# /error-states $ARGUMENTS

Use the `error-states` skill for this request: $ARGUMENTS

If no details were provided, ask for what the skill needs first, then follow the skill's process exactly.
